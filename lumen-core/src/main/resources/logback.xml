<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%15.15thread] %cyan(%-40.40logger{39}) : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Tomcat / Catalina internals -->
    <logger name="org.apache" level="WARN"/>

    <!-- Hibernate internals — connection pool warnings are expected on startup -->
    <logger name="org.hibernate" level="WARN"/>

    <!-- Jakarta Mail / Angus Mail internals -->
    <logger name="jakarta.mail" level="WARN"/>
    <logger name="com.sun.mail" level="WARN"/>
    <logger name="org.eclipse.angus" level="WARN"/>

    <!-- HikariCP — pool lifecycle at WARN only -->
    <logger name="com.zaxxer.hikari" level="WARN"/>

    <!-- Lumen framework -->
    <logger name="io.lumen" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>